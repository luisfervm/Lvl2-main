tasks:
  Run all tasks:
    shell: python -m robot --report NONE --outputdir output --logtitle "MainTaskLvl2" tasks.robot

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
